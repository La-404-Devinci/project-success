{% extends 'base.html.twig' %}

{% block title %}{{ news.title }}• Success Project{% endblock %}

{% block body %}
    <main class="w-11/12 lg:w-2/3 mx-auto my-12 flex flex-col gap-8 shadow-md rounded-lg bg-white">
        <div class="flex flex-col gap-4">
            <img src="/assets/uploads/{{ news.img }}" alt="" class="w-full h-[150px] sm:h-[300px] object-cover rounded-lg">
            <h1 class="text-3xl font-medium text-center">{{ news.title }}</h1>
        </div>
        <div class="w-full lg:w-2/3 mx-auto leading-7">
            {{ news.text | raw }}
        </div>

        <div>

        </div>
    </main>
{% endblock %}