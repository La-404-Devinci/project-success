{% extends 'base.html.twig' %}

{% block title %}{{ news.title }}• Success Project{% endblock %}

{% block body %}
    <main class="w-2/3 mx-auto my-12 flex flex-col gap-8">
        <div class="flex flex-col gap-4">
            <img src="{{ news.img }}" alt="" class="w-full h-[300px] object-cover rounded-lg">
            <h1 class="text-3xl font-medium text-center">{{ news.title }}</h1>
        </div>
        <div class="w-2/3 mx-auto">
            {{ news.text }}
        </div>

        <div>
            {{ }}
        </div>
    </main>
{% endblock %}