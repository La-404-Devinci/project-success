{% extends 'base.html.twig' %}

{% block title %}News • Success Project{% endblock %}

{% block body %}
<main class="w-11/12 h-full lg:w-9/12 mx-auto">
    <section class="w-full grid grid-cols-3 gap-4">
        {% for article in news %}
            <article>
                <img src="{{ article.img }}" alt="" class="h-9/12">
                <div>
                    <h2>{{ article.title }}</h2>
                </div>
            </article>
        {% endfor %}
    </section>
</main>
{% endblock %}
